[package]
name = "corgi-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.49.0", features = ["full"]}
sqlx = {version = "0.8.6", features = ["runtime-tokio", "sqlite", "macros"]}
chrono = "0.4.43"

[build-dependencies]
dotenv = "0.15.0"
