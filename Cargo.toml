[package]
name = "corgi-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = {version = "0.8.6", features = ["runtime-tokio", "sqlite", "macros"]}
chrono = "0.4.43"

[build-dependencies]
dotenv = "0.15.0"

[dev-dependencies]
tokio = {version = "1.49.0", features = ["full"]}
